# Player-visible read scope

This directory intentionally contains no adventure, encounter, campaign, or
rules content. It exists only so the typical-player profile can resolve one
harmless, narrowly scoped tool instead of relying on an empty tool list.
